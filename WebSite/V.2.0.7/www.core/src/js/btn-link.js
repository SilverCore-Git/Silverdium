/*
/ @autor = Mister Papaye
/ @autor = SivlerCore
*/

// service btn link //

let silverdiumurl = 'https://silverdium.fr';
let silversounddetector = '#silversounddetector';

function silverdium() {
    window.location.href = `${silverdiumurl}`
};

let gitssd = 'https://github.com/SilverCore-Git/Silver-SoundDetector';

function git(repo) {
    if (repo === 'ssd') {
        window.location.href = gitssd;
    };
}